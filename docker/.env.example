# -----------------------------
# Host ports (mapped from Docker)
# -----------------------------
MYSQL_EXPOSE_PORT=3316
REDIS_EXPOSE_PORT=6389
API_EXPOSE_PORT=3000
WEB_EXPOSE_PORT=3001

# -----------------------------
# MySQL (container init)
# -----------------------------
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=linknest
MYSQL_USER=linknest
MYSQL_PASSWORD=linknest

# -----------------------------
# Redis (container init)
# -----------------------------
REDIS_PASSWORD=

# -----------------------------
# API runtime
# -----------------------------
JWT_SECRET=secret-key
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=refresh-secret-key
JWT_REFRESH_EXPIRES_IN=30d

SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=

# -----------------------------
# Web runtime
# -----------------------------
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

